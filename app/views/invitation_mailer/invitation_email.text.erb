You've been invited to join "<%= @team.name %>"

Hi there!

<%= @invited_by.email %> has invited you to join their team "<%= @team.name %>".

<% if @user_exists %>
Since you already have an account, you can accept this invitation right away by visiting:
<% else %>
To join this team, you'll need to create an account first. Get started by visiting:
<% end %>

<%= @invitation_url %>

Note: This invitation will expire on <%= @invitation.expires_at.strftime("%B %d, %Y at %I:%M %p") %>.

If you don't want to join this team, you can simply ignore this email.

---
This invitation was sent by <%= @invited_by.email %>
